## src/pages 技术债/陈旧要素盘点

- **HomePage.tsx**
  - 组件体量过大，集成登录、应用列表、背景切换、调试入口等多职责，缺少拆分与状态集中管理。
  - 大量内联样式和硬编码文案/路由，复用性差且难以统一主题。
  - API 应用和静态应用的跳转逻辑直接操作 `window.location`，缺少失败兜底与可测试性。

- **DashboardPage.tsx**
  - 背景与字体样式硬编码在组件内，缺少全局主题/布局复用。
  - 未登录时仅返回静态卡片，没有统一的鉴权路由保护与重定向策略。

- **LoginPage.tsx**
  - 背景样式内联，返回主页按钮与 RegisterPage 重复实现，可抽取复用组件。
  - `loginMutation` 的错误类型为 `any`，缺少类型约束与统一错误提示。

- **RegisterPage.tsx**
  - 与 LoginPage 共享的大量 UI/背景/按钮代码未抽象成组件。
  - 成功/失败提示及加载状态分散在本地 state，缺少统一的 form 状态管理或通知体系。

- **AppsPage.tsx**
  - 组件同时承担应用展示、NKey 生成、用户资料更新等多职责，状态复杂且未模块化。
  - `pingUrl` 采用前端 HEAD 请求 + 5s 超时，未做中断/清理，在弱网下可能悬挂。
  - 多处使用 `alert`/`window.location.href` 进行错误提示和跳转，不利于可测试性与无障碍。
  - 背景、栅格样式硬编码且与主页样式重复，缺少复用层。

- **AppLauncherPage.tsx**
  - 页面内部重复获取用户应用列表（Home 已获取），无缓存或注入，增加网络开销。
  - 针对未登录或缺少 appId 的情形仅设置状态，未统一重定向或提示。
  - 未对 `useEffect` 中的异步请求做取消处理。

- **SongPicQueryPage.tsx**
  - `navigator.clipboard` 复制无降级方案，HTTP 环境或 Safari 下可能失败。
  - `objectUrls` 仅在卸载时清理，未在新图片加载时回收，可能导致内存泄漏。
  - 大量内联样式与组件逻辑混杂，缺少拆分（列表/控制面板/预览）。

- **FreedomConstPage.tsx**
  - 超大单文件组件，集成数据拉取、筛选、画布编辑、导出等多职责，维护性差。
  - 外部数据源、背景、难度配色等均硬编码且跨域，缺少缓存和失败处理。
  - 依赖 `html2canvas` 和 `contentEditable`，未处理跨域图片/文本同步的边界，易导出失败。
  - 拖拽/旋转/镜像等状态直接存储在 `useState`，缺少集中管理（如 reducer）和历史/撤销能力。
  - @Copilot commented on this pull request.

Pull request overview

This pull request introduces Version 1.0.0 of the FreedomConstPage, adding a significant new edit mode feature that allows users to manually reposition items via drag-and-drop, along with various UI improvements and bug fixes.

Changes:

Added edit mode with drag-and-drop functionality for fine-tuning item positions (offsetX/offsetY)
Updated difficulty colors for better visual consistency and improved special item selection with toggle behavior
Enhanced error handling to suppress common non-critical errors (Script error, ResizeObserver loop)
Reviewed changes

Copilot reviewed 4 out of 8 changed files in this pull request and generated 11 comments.

Show a summary per file
File	Description
src/types/freedomConst.ts	Added offsetX and offsetY fields to BlockItem interface for edit mode positioning
src/pages/FreedomConstPage.tsx	Major feature addition: edit mode with drag-and-drop, UI improvements including difficulty color changes, toggle selection for special items, sticky sidebar, improved item controls layout, and code formatting
src/components/ErrorBoundary.tsx	Added filtering to suppress common non-critical errors from triggering error boundary
src/pages/AboutMePage.tsx	Changed profile icon from SVG to PNG format
src/assets/images/icons/icon.png	New PNG icon file added
Comments suppressed due to low confidence (1)
src/pages/FreedomConstPage.tsx:332

The export function should hide or disable edit mode controls and visual indicators before capturing the image. Currently, if a user exports while in edit mode, the cursor styles and selection prevention may affect the exported image appearance. Consider temporarily disabling edit mode or ensuring edit-mode-specific styles are not captured.
  const handleExport = async () => {
    if (!tableRef.current) return;
    try {
      const canvas = await html2canvas(tableRef.current, {
        scale: 2,
        useCORS: true,
        allowTaint: true,
        onclone: (clonedDoc: Document) => {
          const inputs = clonedDoc.querySelectorAll('input');
          inputs.forEach((input: HTMLInputElement) => {
            const div = clonedDoc.createElement('div');
            div.innerText = input.value;
            div.style.cssText = window.getComputedStyle(input).cssText;
            div.style.display = 'flex';
            div.style.alignItems = 'center';
            div.style.justifyContent = 'center';
            // Ensure font size is copied correctly if it was inline
            if (input.style.fontSize) {
              div.style.fontSize = input.style.fontSize;
            }
            input.parentNode?.replaceChild(div, input);
          });

          const textareas = clonedDoc.querySelectorAll('textarea');
          textareas.forEach((textarea: HTMLTextAreaElement) => {
            const div = clonedDoc.createElement('div');
            div.innerText = textarea.value;
            div.style.cssText = window.getComputedStyle(textarea).cssText;
            div.style.display = 'flex';
            div.style.alignItems = 'center';
            div.style.justifyContent = 'center';
            div.style.whiteSpace = 'pre-wrap';
            if (textarea.style.fontSize) {
              div.style.fontSize = textarea.style.fontSize;
            }
            if (textarea.style.paddingTop) {
              div.style.paddingTop = textarea.style.paddingTop;
            }
            textarea.parentNode?.replaceChild(div, textarea);
          });
        }
      } as any);
      const link = document.createElement('a');
      link.download = `freedom-const-${Date.now()}.jpg`;
      link.href = canvas.toDataURL('image/jpeg');
      link.click();
    } catch (err) {
      console.error('Export failed:', err);
      alert('导出失败，请检查控制台错误。可能由于跨域图片限制。');
    }
  };
💡 Add Copilot custom instructions for smarter, more guided reviews. Learn how to get started.

In src/pages/FreedomConstPage.tsx:

> +          textareas.forEach((textarea: HTMLTextAreaElement) => {
+            const div = clonedDoc.createElement('div');
+            div.innerText = textarea.value;
+            div.style.cssText = window.getComputedStyle(textarea).cssText;
+            div.style.display = 'flex';
+            div.style.alignItems = 'center';
+            div.style.justifyContent = 'center';
+            div.style.whiteSpace = 'pre-wrap';
+            if (textarea.style.fontSize) {
+              div.style.fontSize = textarea.style.fontSize;
+            }
+            if (textarea.style.paddingTop) {
+              div.style.paddingTop = textarea.style.paddingTop;
+            }
+            textarea.parentNode?.replaceChild(div, textarea);
+          });
The contentEditable divs for both song titles and text items need to handle the onclone callback during export. Currently, the export function only handles input and textarea elements, but contentEditable divs will not have their edited content properly captured. Add logic to handle contentEditable elements in the onclone callback to ensure edited text is correctly exported.

⬇️ Suggested change
-          });
+          });
+
+          // Ensure contentEditable elements (e.g., song titles and text items)
+          // have their latest edited content reflected in the cloned document.
+          const originalEditableElements = document.querySelectorAll<HTMLElement>('[contenteditable="true"]');
+          const clonedEditableElements = clonedDoc.querySelectorAll<HTMLElement>('[contenteditable="true"]');
+
+          clonedEditableElements.forEach((editable, index) => {
+            const original = originalEditableElements[index];
+            if (original) {
+              // Copy the full HTML content so formatting within contentEditable is preserved.
+              editable.innerHTML = original.innerHTML;
+            }
+          });
In src/pages/FreedomConstPage.tsx:

> +  const handleMouseDown = (e: React.MouseEvent, blockId: string, itemId: string) => {
+    if (!isEditMode) return;
+
+    e.preventDefault();
+    e.stopPropagation();
+
+    // Find current item to get initial offsets
+    const block = blocks.find(b => b.id === blockId);
+    const item = block?.items.find(i => i.id === itemId);
+    const initialOffsetX = item?.offsetX || 0;
+    const initialOffsetY = item?.offsetY || 0;
+    const startX = e.clientX;
+    const startY = e.clientY;
+
+    const onMouseMove = (moveEvent: MouseEvent) => {
+      const deltaX = moveEvent.clientX - startX;
+      const deltaY = moveEvent.clientY - startY;
+
+      handleUpdateItem(blockId, itemId, {
+        offsetX: initialOffsetX + deltaX,
+        offsetY: initialOffsetY + deltaY
+      });
+    };
+
+    const onMouseUp = () => {
+      document.removeEventListener('mousemove', onMouseMove);
+      document.removeEventListener('mouseup', onMouseUp);
+    };
+
+    document.addEventListener('mousemove', onMouseMove);
+    document.addEventListener('mouseup', onMouseUp);
+  };
+
+  const handleTouchStart = (e: React.TouchEvent, blockId: string, itemId: string) => {
+    if (!isEditMode) return;
+
+    if (e.touches.length !== 1) return;
+    e.stopPropagation();
+
+    // Find current item to get initial offsets
+    const block = blocks.find(b => b.id === blockId);
+    const item = block?.items.find(i => i.id === itemId);
+    const initialOffsetX = item?.offsetX || 0;
+    const initialOffsetY = item?.offsetY || 0;
+    const startX = e.touches[0].clientX;
+    const startY = e.touches[0].clientY;
+
+    const onTouchMove = (moveEvent: TouchEvent) => {
+      if (moveEvent.cancelable) moveEvent.preventDefault(); // Prevent scrolling
+      const deltaX = moveEvent.touches[0].clientX - startX;
+      const deltaY = moveEvent.touches[0].clientY - startY;
+
+      handleUpdateItem(blockId, itemId, {
+        offsetX: initialOffsetX + deltaX,
+        offsetY: initialOffsetY + deltaY
+      });
+    };
+
+    const onTouchEnd = () => {
+      document.removeEventListener('touchmove', onTouchMove);
+      document.removeEventListener('touchend', onTouchEnd);
+    };
+
+    document.addEventListener('touchmove', onTouchMove, { passive: false });
+    document.addEventListener('touchend', onTouchEnd);
+  };
The drag-and-drop event handlers may leak memory if the component unmounts while a drag is in progress. When the user exits edit mode or the component unmounts during an active drag operation, the mousemove/touchmove and mouseup/touchend event listeners will remain attached to the document object. Consider adding a cleanup effect that removes these listeners when isEditMode changes or the component unmounts.

In src/pages/FreedomConstPage.tsx:

> +                                        >
+                                          {item.song?.title || 'Title'}
The contentEditable elements for song titles and text items don't save changes to state. While there's an onBlur handler for text items (line 860), song titles (line 832) have no handler, meaning edits will be lost on re-render. Add an onBlur handler to persist title changes to state.

⬇️ Suggested change
-                                        >
-                                          {item.song?.title || 'Title'}
+                                          onBlur={(e) => handleUpdateItem(block.id, item.id, { title: e.currentTarget.innerText })}
+                                        >
+                                          {item.title ?? item.song?.title || 'Title'}
In src/pages/FreedomConstPage.tsx:

> @@ -303,20 +373,20 @@ export default function FreedomConstPage() {
 
           <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
             {/* Left Sidebar */}
-            <div className="lg:col-span-1 space-y-6">
+            {/* Left Sidebar */}
+            <div className="lg:col-span-1 space-y-6 sticky top-24 self-start">
The sticky sidebar (line 377) uses 'top-24' but there's a fixed back button at 'top-5'. On smaller viewports or when scrolling, the sidebar might overlap with the back button. Verify that the spacing is adequate across different screen sizes and scroll positions.

⬇️ Suggested change
-            <div className="lg:col-span-1 space-y-6 sticky top-24 self-start">
+            <div className="lg:col-span-1 space-y-6 sticky top-32 lg:top-24 self-start">
In src/pages/FreedomConstPage.tsx:

> +    const onMouseMove = (moveEvent: MouseEvent) => {
+      const deltaX = moveEvent.clientX - startX;
+      const deltaY = moveEvent.clientY - startY;
+
+      handleUpdateItem(blockId, itemId, {
+        offsetX: initialOffsetX + deltaX,
+        offsetY: initialOffsetY + deltaY
+      });
The drag handlers call handleUpdateItem on every mousemove/touchmove event, which triggers a state update and re-render on each pixel moved. This can cause performance issues during dragging, especially with many items. Consider throttling the updates or using a temporary visual transform during dragging and only updating state on mouseup/touchend.

In src/pages/FreedomConstPage.tsx:

> @@ -303,20 +373,20 @@ export default function FreedomConstPage() {
 
           <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
             {/* Left Sidebar */}
-            <div className="lg:col-span-1 space-y-6">
+            {/* Left Sidebar */}
There is a duplicate comment 'Left Sidebar' on consecutive lines. Remove one of these duplicate comments.

⬇️ Suggested change
-            {/* Left Sidebar */}
In src/pages/FreedomConstPage.tsx:

> @@ -124,7 +125,7 @@ export default function FreedomConstPage() {
   const handleAddBlock = (index?: number) => {
     const newBlock: ConstBlock = {
       id: Date.now().toString(),
-      label: '???',
+      label: '??',
The default label for new blocks has been changed from '???' to '??'. Verify that this was intentional and not a typo. The shorter label may impact visual consistency with existing blocks or user expectations.

⬇️ Suggested change
-      label: '??',
+      label: '???',
In src/pages/FreedomConstPage.tsx:

> @@ -337,8 +407,15 @@ export default function FreedomConstPage() {
                   <Button onClick={handleExport} className="flex-1 flex items-center justify-center gap-2 bg-[#667eea] hover:bg-[#5a6fd6]">
                     <Download size={16} /> 保存图片
                   </Button>
-                  <Button 
-                    onClick={() => setIsTitleTruncated(!isTitleTruncated)} 
+                  <Button
+                    onClick={() => setIsEditMode(!isEditMode)}
+                    className={`flex-none px-3 border ${isEditMode ? 'bg-blue-100 text-blue-600 border-blue-200' : 'bg-white/50 text-gray-700 border-gray-200'} hover:bg-white/80`}
The edit mode button's visual indicator uses inline conditional styling with long template literals, making it difficult to maintain. Consider extracting this to a computed className variable or using a utility function for better readability.

In src/pages/FreedomConstPage.tsx:

> @@ -337,8 +407,15 @@ export default function FreedomConstPage() {
                   <Button onClick={handleExport} className="flex-1 flex items-center justify-center gap-2 bg-[#667eea] hover:bg-[#5a6fd6]">
                     <Download size={16} /> 保存图片
                   </Button>
-                  <Button 
-                    onClick={() => setIsTitleTruncated(!isTitleTruncated)} 
+                  <Button
+                    onClick={() => setIsEditMode(!isEditMode)}
+                    className={`flex-none px-3 border ${isEditMode ? 'bg-blue-100 text-blue-600 border-blue-200' : 'bg-white/50 text-gray-700 border-gray-200'} hover:bg-white/80`}
+                    title={isEditMode ? "退出编辑模式" : "进入编辑模式"}
The edit mode pen icon button lacks accessible keyboard navigation context. Consider adding an aria-label attribute in addition to the title attribute to better support screen readers, especially since this is a new feature that changes the interaction mode of the entire interface.

⬇️ Suggested change
-                    title={isEditMode ? "退出编辑模式" : "进入编辑模式"}
+                    title={isEditMode ? "退出编辑模式" : "进入编辑模式"}
+                    aria-label={isEditMode ? "退出编辑模式" : "进入编辑模式"}
In src/components/ErrorBoundary.tsx:

>  
   React.useEffect(() => {
     const handleError = (event: ErrorEvent) => {
+      // 忽略常见的非关键性错误
+      if (
+        event.message === 'Script error.' || 
+        event.message.includes('Script error') ||
+        event.message.includes('ResizeObserver loop')
+      ) {
+        console.warn('Ignored non-critical error:', event.message);
The error filtering logic in the ErrorBoundary silently suppresses 'Script error.' and 'ResizeObserver loop' errors. While these are often non-critical, completely suppressing them may hide legitimate issues in production. Consider logging these to an error tracking service or keeping a count of suppressed errors to detect patterns that might indicate real problems.

⬇️ Suggested change
-
-  React.useEffect(() => {
-    const handleError = (event: ErrorEvent) => {
-      // 忽略常见的非关键性错误
-      if (
-        event.message === 'Script error.' || 
-        event.message.includes('Script error') ||
-        event.message.includes('ResizeObserver loop')
-      ) {
-        console.warn('Ignored non-critical error:', event.message);
+  const ignoredErrorCountRef = React.useRef(0);
+
+  React.useEffect(() => {
+    const handleError = (event: ErrorEvent) => {
+      // 忽略常见的非关键性错误，但仍然进行基本统计/记录
+      if (
+        event.message === 'Script error.' || 
+        event.message.includes('Script error') ||
+        event.message.includes('ResizeObserver loop')
+      ) {
+        ignoredErrorCountRef.current += 1;
+        console.warn(
+          'Ignored non-critical error:',
+          event.message,
+          `Ignored count: ${ignoredErrorCountRef.current}`,
+        );
+        const trackingService = (window as any).errorTrackingService;
+        if (trackingService && typeof trackingService.captureMessage === 'function') {
+          trackingService.captureMessage('Ignored non-critical error', {
+            level: 'warning',
+            extra: {
+              message: event.message,
+              ignoredCount: ignoredErrorCountRef.current,
+            },
+          });
+        }
In src/pages/FreedomConstPage.tsx:

> -                                              key={key}
-                                              onClick={() => handleUpdateItem(block.id, item.id, { difficulty: key })}
-                                              className="w-full h-4 text-[8px] flex items-center justify-center text-white rounded-[1px]"
-                                              style={{ backgroundColor: diff.color }}
-                                              title={diff.label}
-                                          >
-                                              {key[0].toUpperCase()}
+                                  <div
+                                    className={`relative group/item w-24 ${isEditMode ? 'cursor-move z-20 hover:z-30' : ''}`}
+                                    style={{
+                                      transform: `translate(${item.offsetX || 0}px, ${item.offsetY || 0}px)`,
+                                      touchAction: isEditMode ? 'none' : 'auto',
+                                      userSelect: isEditMode ? 'none' : 'auto',
+                                      WebkitUserSelect: isEditMode ? 'none' : 'auto',
+                                      WebkitTouchCallout: isEditMode ? 'none' : 'default'
+                                    }}
The drag-and-drop functionality in edit mode is not accessible via keyboard. Users who rely on keyboard navigation cannot reposition items. Consider implementing keyboard shortcuts (e.g., arrow keys) to adjust item positions when in edit mode, or provide an alternative interface for adjusting offsets.

⬇️ Suggested change
-                                    }}
+                                    }}
+                                    tabIndex={isEditMode ? 0 : -1}
+                                    role="button"
+                                    aria-roledescription="draggable item"
+                                    aria-label={isEditMode ? '可拖动项目。使用方向键微调位置。' : '项目'}
+                                    onKeyDown={(e) => {
+                                      if (!isEditMode) return;
+                                      const step = 5;
+                                      let currentTransform = e.currentTarget.style.transform;
+                                      if (!currentTransform) {
+                                        currentTransform = `translate(${item.offsetX || 0}px, ${item.offsetY || 0}px)`;
+                                      }
+                                      const match = currentTransform.match(/translate\(([-\d.]+)px,\s*([-\d.]+)px\)/);
+                                      let x = 0;
+                                      let y = 0;
+                                      if (match) {
+                                        x = parseFloat(match[1]) || 0;
+                                        y = parseFloat(match[2]) || 0;
+                                      }
+                                      let handled = false;
+                                      switch (e.key) {
+                                        case 'ArrowLeft':
+                                          x -= step;
+                                          handled = true;
+                                          break;
+                                        case 'ArrowRight':
+                                          x += step;
+                                          handled = true;
+                                          break;
+                                        case 'ArrowUp':
+                                          y -= step;
+                                          handled = true;
+                                          break;
+                                        case 'ArrowDown':
+                                          y += step;
+                                          handled = true;
+                                          break;
+                                        default:
+                                          break;
+                                      }
+                                      if (handled) {
+                                        e.preventDefault();
+                                        e.currentTarget.style.transform = `translate(${x}px, ${y}px)`;
+                                      }
+                                    }}
—